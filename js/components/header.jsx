var React = require("react");

var Header = React.createClass({
    render: function () {
        return (
            <header>
                <h1>React iTunes Artwork</h1>
            </header>
        )
    }
});

module.exports = Header;